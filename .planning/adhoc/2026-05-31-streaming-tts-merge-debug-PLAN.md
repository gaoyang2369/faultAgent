---
mode: debug
date: 2026-05-31
task: streaming-tts-merge
---

# 流式播报合并调试计划

## 目标

同步远端最新代码，保留本地消息编辑与语音打断能力，并将远端流式 TTS 原型调整为可靠的串行合成队列。

## 范围

- 快进同步远端分支，通过 stash 保留本地未提交修改。
- 保留语音网关返回音频时的直接播放链路。
- 为普通文字回复增加可控的流式播报开关。
- 增加文本清理、按标点和长度切段、串行合成以及主动中止能力。
- 增加后端 `/tts/synthesize` 代理接口和配置项。

## 验证

- 检查无残留冲突标记。
- 运行前端文本切段测试与生产构建。
- 运行 TTS 代理接口测试及相关回归测试。
- 运行 `git diff --check`。

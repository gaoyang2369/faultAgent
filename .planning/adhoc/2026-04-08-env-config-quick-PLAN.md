# .env 数据库参数更新计划

日期：2026-04-08

## 目标

- 按用户最新确认的参数更新 `.env` 中的 MySQL / DCMA 配置。
- 不臆造 PostgreSQL 本地参数，保留现有本地占位配置。

## 执行项

1. 将 `HOST` 更新为老师/学长提供的 MySQL 地址。
2. 将 `MYSQL_PW`、`DB_NAME` 更新为当前确认值。
3. 新增 `DCMA_DB_NAME`，并按用户要求暂时视为库名使用。
4. 变更后复核 `.env` 的相关键值。

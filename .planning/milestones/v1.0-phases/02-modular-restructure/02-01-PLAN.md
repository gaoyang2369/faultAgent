---
phase: 02-modular-restructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.py
  - utils.py
  - tests/test_config.py
  - tests/test_utils.py
autonomous: true
requirements: [CONF-01, CONF-02]

must_haves:
  truths:
    - "config.py exists at project root with all 8 hardcoded values as module-level constants"
    - "utils.py exists at project root with sanitize_for_json, safe_json_dumps, parse_todos_from_tool_output exported"
    - "config.py has no side effects beyond load_dotenv and os.getenv (no network, no DB, no file I/O)"
    - "utils.py has no imports from app.py, tools.py, or config.py (only stdlib + langchain_core.messages)"
    - "Unit tests for config.py and utils.py pass"
  artifacts:
    - path: "config.py"
      provides: "Centralized configuration constants"
      contains: "OLLAMA_BASE_URL"
    - path: "utils.py"
      provides: "Generic utility functions"
      exports: ["sanitize_for_json", "safe_json_dumps", "parse_todos_from_tool_output"]
    - path: "tests/test_config.py"
      provides: "Config module unit tests"
      contains: "test_"
    - path: "tests/test_utils.py"
      provides: "Utils module unit tests"
      contains: "test_"
  key_links:
    - from: "config.py"
      to: ".env"
      via: "load_dotenv + os.getenv"
      pattern: "os\\.getenv"
    - from: "utils.py"
      to: "langchain_core.messages"
      via: "import for type checking in sanitize_for_json"
      pattern: "from langchain_core.messages import"
---

<objective>
Create the two new foundation modules (config.py and utils.py) that Phase 2 introduces, along with their unit tests.

Purpose: These modules provide the centralized configuration and generic utilities that existing files (app.py, tools.py, knowledge_base.py) will import from in Plan 02. Creating them first with tests ensures the contracts are solid before rewiring.

Output: config.py, utils.py, tests/test_config.py, tests/test_utils.py -- all new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-modular-restructure/02-CONTEXT.md
@.planning/phases/02-modular-restructure/02-RESEARCH.md

Source files to extract from:
@tools.py (lines 389-597: utility functions and todo parsing)
@knowledge_base.py (lines 24-26, 73-75: hardcoded Ollama URL, embedding model, FAISS path)
@app.py (lines 245-246, 321: hardcoded max_tokens, messages_to_keep, recursion_limit)
@subagent/call_api_tool.py (line 91: hardcoded fault API URL)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create config.py with all 8 hardcoded values</name>
  <files>config.py, tests/test_config.py</files>
  <read_first>
    - knowledge_base.py (lines 24-26, 73-75 for OLLAMA_BASE_URL, EMBEDDING_MODEL values)
    - app.py (lines 245-246 for max_tokens/messages_to_keep, line 321 for recursion_limit)
    - tools.py (line 35 for DCMA db_name)
    - subagent/call_api_tool.py (line 91 for fault API URL)
    - tests/conftest.py (to understand test environment setup)
  </read_first>
  <behavior>
    - Test: `from config import OLLAMA_BASE_URL` succeeds and default is `"http://10.108.13.254:11434"`
    - Test: `from config import EMBEDDING_MODEL` succeeds and default is `"qwen3-embedding:8b"`
    - Test: `from config import FAISS_PATH` succeeds and default is `"faiss_db"`
    - Test: `from config import MAX_TOKENS_BEFORE_SUMMARY` succeeds and default is `64000` (int, not str)
    - Test: `from config import MESSAGES_TO_KEEP` succeeds and default is `20` (int, not str)
    - Test: `from config import RECURSION_LIMIT` succeeds and default is `50` (int, not str)
    - Test: `from config import DCMA_DB_NAME` succeeds and default is `"dcma"`
    - Test: `from config import FAULT_API_URL` succeeds and default is `"http://10.108.13.250:8001/predict_reason"`
    - Test: Setting env var `OLLAMA_BASE_URL=http://custom:1234` before import overrides the default (use monkeypatch or os.environ + importlib.reload)
    - Test: Setting env var `MAX_TOKENS_BEFORE_SUMMARY=32000` results in int 32000, not string "32000"
  </behavior>
  <action>
    **Create config.py at project root** with this exact structure:

    ```python
    """Centralized configuration for the fault diagnosis agent system.

    All hardcoded values are collected here. Non-secret values have defaults
    matching the original hardcoded behavior. Secret values (DB passwords,
    API keys) are loaded from .env only.
    """
    import os
    from dotenv import load_dotenv

    load_dotenv(override=True)

    # === Knowledge Base (Ollama + FAISS) ===
    OLLAMA_BASE_URL = os.getenv("OLLAMA_BASE_URL", "http://10.108.13.254:11434")
    EMBEDDING_MODEL = os.getenv("EMBEDDING_MODEL", "qwen3-embedding:8b")
    FAISS_PATH = os.getenv("FAISS_PATH", "faiss_db")

    # === Agent Behavior ===
    MAX_TOKENS_BEFORE_SUMMARY = int(os.getenv("MAX_TOKENS_BEFORE_SUMMARY", "64000"))
    MESSAGES_TO_KEEP = int(os.getenv("MESSAGES_TO_KEEP", "20"))
    RECURSION_LIMIT = int(os.getenv("RECURSION_LIMIT", "50"))

    # === Database ===
    DCMA_DB_NAME = os.getenv("DCMA_DB_NAME", "dcma")

    # === External APIs ===
    FAULT_API_URL = os.getenv("FAULT_API_URL", "http://10.108.13.250:8001/predict_reason")
    ```

    Key rules:
    - Module-level constants only. NO class, NO dataclass, NO pydantic-settings (user explicitly deferred).
    - `load_dotenv(override=True)` at top -- called once when config is first imported.
    - Defaults for non-secret values match the exact hardcoded values found in source files.
    - `int()` conversion for the 3 numeric values (MAX_TOKENS_BEFORE_SUMMARY, MESSAGES_TO_KEEP, RECURSION_LIMIT).
    - No network calls, no DB connections, no file I/O beyond reading .env.

    **Create tests/test_config.py** with TDD tests:
    - Test all 8 constants have expected default values (when env vars are NOT set for those keys).
    - Test that env var override works: use `monkeypatch.setenv` + `importlib.reload(config)` to verify override.
    - Test that numeric values are `int` type, not `str`.
    - Important: tests/conftest.py already sets some env vars via `os.environ.setdefault()`. Since `setdefault` does NOT overwrite existing values, your test for default values should work correctly for keys NOT in _TEST_ENV (like OLLAMA_BASE_URL). For keys that ARE in _TEST_ENV (like MODEL_NAME), don't test default values -- they'll reflect the test env.

    Write RED tests first, then create config.py, then verify GREEN.
  </action>
  <verify>
    <automated>cd "/Users/neuron/文稿/2 私人/fault-diagnosis" && python -m pytest tests/test_config.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - config.py exists at project root
    - config.py contains `OLLAMA_BASE_URL = os.getenv("OLLAMA_BASE_URL", "http://10.108.13.254:11434")`
    - config.py contains `EMBEDDING_MODEL = os.getenv("EMBEDDING_MODEL", "qwen3-embedding:8b")`
    - config.py contains `FAISS_PATH = os.getenv("FAISS_PATH", "faiss_db")`
    - config.py contains `MAX_TOKENS_BEFORE_SUMMARY = int(os.getenv("MAX_TOKENS_BEFORE_SUMMARY", "64000"))`
    - config.py contains `MESSAGES_TO_KEEP = int(os.getenv("MESSAGES_TO_KEEP", "20"))`
    - config.py contains `RECURSION_LIMIT = int(os.getenv("RECURSION_LIMIT", "50"))`
    - config.py contains `DCMA_DB_NAME = os.getenv("DCMA_DB_NAME", "dcma")`
    - config.py contains `FAULT_API_URL = os.getenv("FAULT_API_URL", "http://10.108.13.250:8001/predict_reason")`
    - config.py contains `from dotenv import load_dotenv` and `load_dotenv(override=True)`
    - config.py does NOT contain `class ` or `@dataclass` or `BaseSettings`
    - tests/test_config.py exists and `pytest tests/test_config.py -x` exits 0
    - `python -c "from config import OLLAMA_BASE_URL, EMBEDDING_MODEL, FAISS_PATH, MAX_TOKENS_BEFORE_SUMMARY, MESSAGES_TO_KEEP, RECURSION_LIMIT, DCMA_DB_NAME, FAULT_API_URL; print('OK')"` prints OK
  </acceptance_criteria>
  <done>config.py has all 8 configuration constants with correct defaults and env var override support. Unit tests verify defaults and override behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create utils.py with utility functions extracted from tools.py</name>
  <files>utils.py, tests/test_utils.py</files>
  <read_first>
    - tools.py (lines 389-583 for all utility functions to extract)
    - app.py (line 33 to see current import: `from tools import tools, sanitize_for_json, safe_json_dumps, parse_todos_from_tool_output`)
    - tests/conftest.py (to understand test environment)
  </read_first>
  <behavior>
    - Test: `from utils import sanitize_for_json` succeeds
    - Test: `from utils import safe_json_dumps` succeeds
    - Test: `from utils import parse_todos_from_tool_output` succeeds
    - Test: `sanitize_for_json(None)` returns `None`
    - Test: `sanitize_for_json({"key": "value"})` returns `{"key": "value"}`
    - Test: `sanitize_for_json(datetime(2024, 1, 1))` returns `"2024-01-01T00:00:00"`
    - Test: `sanitize_for_json(HumanMessage(content="hello"))` returns dict with "type", "content", "role" keys
    - Test: `safe_json_dumps({"a": 1})` returns valid JSON string `'{"a": 1}'`
    - Test: `safe_json_dumps` with non-serializable object does NOT raise, returns JSON with error info
    - Test: `parse_todos_from_tool_output(None)` returns `None`
    - Test: `parse_todos_from_tool_output({"todos": [{"content": "task1", "status": "pending"}]})` returns list with normalized todo dict
    - Test: `parse_todos_from_tool_output('{"todos": [{"content": "test", "status": "done"}]}')` parses JSON string and returns normalized list with status "completed"
  </behavior>
  <action>
    **Create utils.py at project root** by extracting functions from tools.py lines 389-583.

    The file must contain these functions in this order:
    1. `sanitize_for_json(obj: Any) -> Any` -- from tools.py lines 390-442. Recursively cleans objects for JSON serialization. Handles None, LangChain message objects (HumanMessage, AIMessage, ToolMessage), dicts, lists/tuples, basic types (str/int/float/bool), datetime objects, and fallback to str().
    2. `safe_json_dumps(obj: Any, ensure_ascii: bool = False) -> str` -- from tools.py lines 445-454. Calls sanitize_for_json then json.dumps. Returns error JSON on failure.
    3. `_normalize_status(value: Any) -> str` -- from tools.py lines 460-481. Maps status strings to "pending"/"in_progress"/"completed". Private helper.
    4. `_extract_bracket_content(text: str, marker: str) -> Optional[str]` -- from tools.py lines 484-503. Extracts bracket content from string. Private helper.
    5. `_normalize_todo_items(raw_todos: List[Any]) -> List[Dict[str, Any]]` -- from tools.py lines 506-524. Normalizes raw todo items to standard format. Private helper.
    6. `_extract_todo_list_from_output(output: Any)` -- from tools.py lines 527-576. Extracts todo list from various output formats. Private helper.
    7. `parse_todos_from_tool_output(output: Any)` -- from tools.py lines 579-583. Public API: extracts and normalizes todos from tool output.

    Import block at top of utils.py:
    ```python
    """Generic utility functions for JSON serialization and todo parsing."""
    import json
    import ast
    import re
    from datetime import datetime
    from typing import Any, Dict, List, Optional

    from langchain_core.messages import HumanMessage, AIMessage, ToolMessage
    ```

    Key rules:
    - Copy functions EXACTLY as they exist in tools.py -- preserve logic, comments (Chinese), and behavior.
    - Do NOT import from tools.py, app.py, or config.py. Only stdlib + langchain_core.messages.
    - Do NOT add any module-level side effects (no load_dotenv, no network calls, no DB).
    - The `from langchain_core.messages import` is used inside `sanitize_for_json` at tools.py line 399. Move it to the module-level import block instead.

    **Create tests/test_utils.py** with TDD tests covering:
    - sanitize_for_json: None, basic types, dict, list, datetime, HumanMessage, nested structures, unserializable objects
    - safe_json_dumps: normal dict, non-serializable fallback
    - parse_todos_from_tool_output: None input, dict with todos key, JSON string input, list input, status normalization ("done" -> "completed", "doing" -> "in_progress")

    Write RED tests first, then create utils.py, then verify GREEN.
  </action>
  <verify>
    <automated>cd "/Users/neuron/文稿/2 私人/fault-diagnosis" && python -m pytest tests/test_utils.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - utils.py exists at project root
    - utils.py contains `def sanitize_for_json(`
    - utils.py contains `def safe_json_dumps(`
    - utils.py contains `def parse_todos_from_tool_output(`
    - utils.py contains `def _normalize_status(`
    - utils.py contains `def _extract_bracket_content(`
    - utils.py contains `def _normalize_todo_items(`
    - utils.py contains `def _extract_todo_list_from_output(`
    - utils.py contains `from langchain_core.messages import HumanMessage, AIMessage, ToolMessage` at module level
    - utils.py does NOT contain `from tools import` or `from app import` or `from config import`
    - utils.py does NOT contain `load_dotenv`
    - tests/test_utils.py exists and `pytest tests/test_utils.py -x` exits 0
    - `python -c "from utils import sanitize_for_json, safe_json_dumps, parse_todos_from_tool_output; print('OK')"` prints OK
  </acceptance_criteria>
  <done>utils.py contains all 7 utility functions extracted from tools.py with identical behavior. Unit tests verify sanitize_for_json, safe_json_dumps, and parse_todos_from_tool_output.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `pytest tests/test_config.py tests/test_utils.py -v` -- all new tests pass
2. `pytest tests/ -x -q` -- all 22 existing tests still pass (config.py and utils.py are additive, no existing code changed)
3. `python -c "from config import OLLAMA_BASE_URL; from utils import sanitize_for_json; print('imports OK')"` -- no circular imports
</verification>

<success_criteria>
- config.py exists with all 8 constants matching exact hardcoded values from source
- utils.py exists with all 7 utility functions copied from tools.py
- Both modules importable independently with no side effects beyond load_dotenv in config.py
- New unit tests pass
- All 22 existing Phase 1 tests still pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/02-modular-restructure/02-01-SUMMARY.md`
</output>

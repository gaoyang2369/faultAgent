---
phase: 05-app-slim-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - streaming.py
  - app.py
autonomous: true
requirements: [SLIM-01, SLIM-02, SLIM-03, SLIM-04]

must_haves:
  truths:
    - "app.py contains no tool definitions, no prompt content, no utility functions, and no SSE streaming logic"
    - "app.py is 300 lines or fewer"
    - "All 76 existing tests pass without modification"
    - "The /chat/stream endpoint returns identical SSE event sequences as before extraction"
    - "PostgreSQL session state persistence works (checkpointer wired in lifespan)"
  artifacts:
    - path: "streaming.py"
      provides: "token_stream_events async generator function"
      exports: ["token_stream_events"]
      min_lines: 100
    - path: "app.py"
      provides: "FastAPI app with lifespan, routes, CORS, static files"
      contains: "from streaming import token_stream_events"
  key_links:
    - from: "app.py"
      to: "streaming.py"
      via: "from streaming import token_stream_events"
      pattern: "from streaming import token_stream_events"
    - from: "streaming.py"
      to: "utils.py"
      via: "from utils import sanitize_for_json, safe_json_dumps, parse_todos_from_tool_output"
      pattern: "from utils import sanitize_for_json, safe_json_dumps, parse_todos_from_tool_output"
    - from: "streaming.py"
      to: "config.py"
      via: "from config import RECURSION_LIMIT"
      pattern: "from config import RECURSION_LIMIT"
    - from: "streaming.py"
      to: "prompts/dynamic_prompt.py"
      via: "from prompts.dynamic_prompt import Context"
      pattern: "from prompts\\.dynamic_prompt import Context"
    - from: "app.py"
      to: "streaming.py"
      via: "token_stream_events(request.app, message, thread_id, user_identity) in stream_chat_log_get"
      pattern: "token_stream_events\\(request\\.app"
---

<objective>
Extract the 134-line `token_stream_events` SSE streaming generator from app.py into a new `streaming.py` module, then verify app.py is under 300 lines and all existing tests pass.

Purpose: Final step of the monolith-to-modules refactoring. After this, app.py contains only its core responsibilities: lifespan management, route handlers, CORS, and static file serving. All domain logic (tools, prompts, middleware, streaming) lives in dedicated modules.

Output: New `streaming.py`, slimmed `app.py` (~267 lines), all 76 tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-app-slim-integration/05-CONTEXT.md

@app.py
@tests/conftest.py
@config.py
@utils.py

<interfaces>
<!-- Key imports the executor needs to understand -->

From app.py (current imports used by token_stream_events):
```python
import asyncio
from typing import AsyncGenerator
from datetime import datetime
from langchain_core.messages import HumanMessage
from utils import sanitize_for_json, safe_json_dumps, parse_todos_from_tool_output
from config import RECURSION_LIMIT
from prompts.dynamic_prompt import Context
```

From app.py (the route handler that calls token_stream_events, stays in app.py):
```python
@app.get("/chat/stream")
async def stream_chat_log_get(request: Request, message: str, thread_id: str = "default", user_identity: str = "游客"):
    # ...
    return StreamingResponse(
        token_stream_events(request.app, message, thread_id, user_identity),
        # ...
    )
```

From tests/conftest.py (mock patches -- none target token_stream_events directly):
```python
with patch("app.AsyncConnectionPool", return_value=mock_pool), \
     patch("app.AsyncPostgresSaver", return_value=mock_checkpointer), \
     patch("app.create_agent", return_value=mock_agent):
```

These patches target `app.*` namespace objects that remain in app.py after extraction. No conftest changes needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create streaming.py with token_stream_events function</name>
  <files>streaming.py</files>
  <read_first>
    - app.py (lines 121-254: the complete token_stream_events function to extract)
    - app.py (lines 1-30: imports currently used by token_stream_events)
    - utils.py (lines 1-8: verify sanitize_for_json, safe_json_dumps, parse_todos_from_tool_output exports)
    - config.py (line 25: verify RECURSION_LIMIT exists)
    - prompts/dynamic_prompt.py (verify Context class is importable)
  </read_first>
  <action>
    Create `streaming.py` in the project root with the following exact content structure:

    **Module docstring:**
    ```python
    """SSE 流式事件生成器 — 从 app.py 提取的 token 级流式输出逻辑"""
    ```

    **Imports (6 lines):**
    ```python
    import asyncio
    from typing import AsyncGenerator
    from datetime import datetime

    from langchain_core.messages import HumanMessage
    from fastapi import FastAPI
    from utils import sanitize_for_json, safe_json_dumps, parse_todos_from_tool_output
    from config import RECURSION_LIMIT
    from prompts.dynamic_prompt import Context
    ```

    **Function:** Copy the entire `token_stream_events` function from app.py lines 121-254 verbatim, with NO modifications to the function body. The function signature is:
    ```python
    async def token_stream_events(app: FastAPI, message: str, thread_id: str, user_identity: str = "游客") -> AsyncGenerator[str, None]:
    ```

    The function body must be an exact copy of lines 122-254 from app.py. Do NOT rename variables, change comments, alter logic, or modify any SSE event format strings. This is a pure move, not a refactor.

    **Total file size:** ~145 lines (docstring + imports + function).
  </action>
  <verify>
    <automated>cd "/Users/neuron/文稿/2 私人/fault-diagnosis" && python -c "from streaming import token_stream_events; print('streaming import OK')" && python -c "import inspect; from streaming import token_stream_events; sig = inspect.signature(token_stream_events); params = list(sig.parameters.keys()); assert params == ['app', 'message', 'thread_id', 'user_identity'], f'Wrong params: {params}'; print('signature OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `streaming.py` exists in the project root
    - `streaming.py` starts with docstring containing "SSE"
    - `streaming.py` contains `from utils import sanitize_for_json, safe_json_dumps, parse_todos_from_tool_output`
    - `streaming.py` contains `from config import RECURSION_LIMIT`
    - `streaming.py` contains `from prompts.dynamic_prompt import Context`
    - `streaming.py` contains `async def token_stream_events(app: FastAPI, message: str, thread_id: str, user_identity: str = "游客") -> AsyncGenerator[str, None]:`
    - `streaming.py` contains `event: start`, `event: token`, `event: tool_start`, `event: tool_end`, `event: complete`, `event: server_error` SSE format strings
    - `streaming.py` contains `from langchain_core.messages import HumanMessage`
    - `python -c "from streaming import token_stream_events"` exits 0
  </acceptance_criteria>
  <done>streaming.py exists with the complete token_stream_events function, importable without errors</done>
</task>

<task type="auto">
  <name>Task 2: Rewire app.py to import from streaming.py and verify all tests pass</name>
  <files>app.py</files>
  <read_first>
    - app.py (full file: current state with token_stream_events at lines 121-254)
    - streaming.py (verify token_stream_events is available from Task 1)
    - tests/conftest.py (verify mock patches at lines 149-151 still target correct app.* paths)
    - tests/test_sse_stream.py (verify tests use HTTP route, not direct function call)
    - tests/test_smoke.py (verify test_app_import_succeeds test)
  </read_first>
  <action>
    **Modify app.py imports — ADD one new import after the existing module imports (after line 29):**
    ```python
    from streaming import token_stream_events
    ```

    **Modify app.py imports — REMOVE now-unused imports from the top of app.py:**
    These are only used by token_stream_events and no longer needed in app.py:
    - Remove `from datetime import datetime` (line 14) — only used in token_stream_events completion_data timestamp
    - Remove `from langchain_core.messages import HumanMessage, AIMessage, SystemMessage` (line 16) — HumanMessage only used in token_stream_events; AIMessage and SystemMessage are not used anywhere in app.py

    **Keep these imports unchanged** (still used by app.py routes/lifespan):
    - `import os` — used in model creation, lifespan DB_URI, static files
    - `import asyncio` — REMOVE: check if used outside token_stream_events. Searching app.py: only used inside token_stream_events (CancelledError). Remove it.
    - `import json` — REMOVE: check if used outside token_stream_events. Searching app.py: not used anywhere else. Remove it.
    - `from typing import AsyncGenerator, Dict, Any, List, Optional` — AsyncGenerator only used by token_stream_events type hint. Keep `Dict, Any, List, Optional` if used elsewhere. Checking: none of these are used in app.py routes. REMOVE the entire typing import line.
    - `from pydantic import BaseModel, Field` — REMOVE: check usage. Not used anywhere in current app.py routes. Remove it.
    - `from fastapi.responses import StreamingResponse, JSONResponse` — KEEP: used in stream_chat_log_get and error handling
    - All other imports (`uvicorn`, `dotenv`, `FastAPI`, `HTTPException`, `Request`, `CORSMiddleware`, `StaticFiles`, `ChatOpenAI`, `create_agent`, `MemorySaver`, `PostgresSaver`, `AsyncPostgresSaver`, `AsyncConnectionPool`, `tools`, `get_sqltools`, `sanitize_for_json`/`safe_json_dumps`/`parse_todos_from_tool_output`, `RECURSION_LIMIT`, `Context`/`identity_aware_prompt`, `build_middleware`) — check each:
      - `from utils import sanitize_for_json, safe_json_dumps, parse_todos_from_tool_output` — REMOVE: only used in token_stream_events
      - `from config import RECURSION_LIMIT` — REMOVE: only used in token_stream_events
      - `from langchain_ollama import ChatOllama` — REMOVE: not used anywhere in app.py
      - `from langgraph.checkpoint.memory import MemorySaver` — REMOVE: not used anywhere in app.py
      - `from langgraph.checkpoint.postgres import PostgresSaver` — REMOVE: not used (only AsyncPostgresSaver is used)

    **Summary of import changes in app.py:**

    REMOVE these import lines entirely:
    - `import asyncio`
    - `import json`
    - `from typing import AsyncGenerator, Dict, Any, List, Optional`
    - `from pydantic import BaseModel, Field`
    - `from datetime import datetime`
    - `from langchain_core.messages import HumanMessage, AIMessage, SystemMessage`
    - `from langchain_ollama import ChatOllama`
    - `from langgraph.checkpoint.memory import MemorySaver`
    - `from langgraph.checkpoint.postgres import PostgresSaver`
    - `from utils import sanitize_for_json, safe_json_dumps, parse_todos_from_tool_output`
    - `from config import RECURSION_LIMIT`

    ADD this import line:
    - `from streaming import token_stream_events`

    KEEP these imports (all still used in app.py):
    - `import os`
    - `import uvicorn`
    - `from dotenv import load_dotenv`
    - `from fastapi import FastAPI, HTTPException, Request`
    - `from fastapi.responses import StreamingResponse, JSONResponse`
    - `from fastapi.middleware.cors import CORSMiddleware`
    - `from starlette.staticfiles import StaticFiles`
    - `from langchain_openai import ChatOpenAI`
    - `from langchain.agents import create_agent`
    - `from langgraph.checkpoint.postgres.aio import AsyncPostgresSaver`
    - `from psycopg_pool import AsyncConnectionPool`
    - `from tools import tools`
    - `from tools.sql_tools import get_sqltools`
    - `from prompts.dynamic_prompt import Context, identity_aware_prompt`
    - `from middleware import build_middleware`
    - `from contextlib import asynccontextmanager`

    **Delete the token_stream_events function (lines 121-254):**
    Remove the entire `async def token_stream_events(...)` function body (134 lines). The route handler `stream_chat_log_get` at line 256 now calls the imported function.

    **Verify the resulting app.py structure:**
    ```
    app.py (~267 lines)
    |- Imports (~18 lines)
    |- load_dotenv (2 lines)
    |- Model creation (16 lines)
    |- Lifespan (54 lines)
    |- FastAPI app + CORS (14 lines)
    |- stream_chat_log_get route (28 lines)
    |- History API routes (30 lines)
    |- Todos API route (47 lines)
    |- Static files mount (12 lines)
    |- Root endpoint (12 lines)
    |- __main__ (11 lines)
    ```

    **Do NOT modify any function body, route handler, or logic.** Only change imports and delete the moved function.
  </action>
  <verify>
    <automated>cd "/Users/neuron/文稿/2 私人/fault-diagnosis" && python -c "from app import app; print('app import OK')" && wc -l app.py | awk '{if ($1 <= 300) print "LINE COUNT OK: "$1; else {print "FAIL: "$1" lines > 300"; exit 1}}' && pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `app.py` contains `from streaming import token_stream_events`
    - `app.py` does NOT contain `async def token_stream_events`
    - `app.py` does NOT contain `import asyncio` (moved to streaming.py)
    - `app.py` does NOT contain `import json` (unused)
    - `app.py` does NOT contain `from typing import` (unused)
    - `app.py` does NOT contain `from pydantic import` (unused)
    - `app.py` does NOT contain `from datetime import` (moved to streaming.py)
    - `app.py` does NOT contain `from langchain_core.messages import` (moved to streaming.py)
    - `app.py` does NOT contain `from langchain_ollama import` (unused)
    - `app.py` does NOT contain `from langgraph.checkpoint.memory import` (unused)
    - `app.py` does NOT contain `from langgraph.checkpoint.postgres import PostgresSaver` (unused; AsyncPostgresSaver is from .aio submodule)
    - `app.py` does NOT contain `from utils import` (moved to streaming.py)
    - `app.py` does NOT contain `from config import` (moved to streaming.py)
    - `app.py` line count is <= 300 (target: ~267)
    - `python -c "from app import app"` exits 0
    - `pytest tests/ -x -q` reports 76 passed, 0 failed
  </acceptance_criteria>
  <done>app.py under 300 lines, imports from streaming.py, no tool definitions/prompt content/utility functions/streaming logic remain, all 76 tests pass</done>
</task>

</tasks>

<verification>
1. `python -c "from streaming import token_stream_events; print('OK')"` -- streaming module importable
2. `python -c "from app import app; print('OK')"` -- app.py imports resolve correctly
3. `wc -l app.py` -- output is <= 300 (SLIM-02)
4. `grep -c "async def token_stream_events" app.py` -- returns 0 (function removed)
5. `grep -c "from streaming import token_stream_events" app.py` -- returns 1 (import added)
6. `grep -c "def extract_data\|def fig_inter\|def python_inter\|def sql_inter\|def query_knowledge_base\|def save_report\|def fault_explanation_tool" app.py` -- returns 0 (SLIM-01: no tool definitions)
7. `grep -c "systemprompt\s*=" app.py` -- returns 0 (SLIM-01: no prompt content)
8. `grep -c "def sanitize_for_json\|def safe_json_dumps\|def parse_todos" app.py` -- returns 0 (SLIM-01: no utility functions)
9. `pytest tests/ -x -q` -- all 76 tests pass (SLIM-03 + SLIM-04)
</verification>

<success_criteria>
- streaming.py exists with token_stream_events function (SLIM-01 partial)
- app.py contains no tool definitions, no prompt content, no utility functions, no streaming logic (SLIM-01 complete)
- app.py is under 300 lines (SLIM-02)
- All 76 existing tests pass unchanged -- SSE events, tool calls, history API, todos API (SLIM-03)
- PostgreSQL checkpointer wired in lifespan, session persistence verified by existing tests (SLIM-04)
</success_criteria>

<output>
After completion, create `.planning/phases/05-app-slim-integration/05-01-SUMMARY.md`
</output>
